apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: tradeup-backend-config
  namespace: tradeup
spec:
  timeoutSec: 86400 # 24 hours for long-lived WebSocket connections
  sessionAffinity:
    affinityType: "GENERATED_COOKIE"
    affinityCookieTtlSec: 86400
