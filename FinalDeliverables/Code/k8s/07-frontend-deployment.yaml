apiVersion: apps/v1
kind: Deployment
metadata:
  name: tradeup-frontend
  namespace: tradeup
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: tradeup-frontend
  template:
    metadata:
      labels:
        app: tradeup-frontend
    spec:
      containers:
        - name: frontend
          image: us-central1-docker.pkg.dev/project-8ca874ae-688d-414c-91b/tradeup/frontend:latest
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_API_URL
              value: "https://api.yourdomain.com" # REPLACE with actual URL
            - name: NEXT_PUBLIC_WS_URL
              value: "https://api.yourdomain.com" # REPLACE with actual URL
            - name: BACKEND_URL
              value: "http://tradeup-backend-svc:80"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
